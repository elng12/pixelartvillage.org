When project updates are made
- Check if the README.md in trickle/notes needs updating
- Update feature lists if new functionality is added
- Update project structure if files are added/removed
- Update technology stack if dependencies change
- Keep usage instructions current with actual functionality